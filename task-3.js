// Calculate all trades per trader for a specified date
// there are two data sets:
// trades = [{traderId: 1, value: -100.00, date: '2016-06-01'}, ...]
// traders [{traderId: 1, name: 'Rob'}, ...]

function calculateTradeTotals(trades, traders, date) {
  var result;
  /// ...
  return result;
}